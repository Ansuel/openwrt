--- a/npu.c
+++ b/npu.c
@@ -447,10 +447,10 @@ int mt76_npu_init(struct mt76_dev *dev,
 
 	mutex_lock(&dev->mutex);
 
-	npu = airoha_npu_get(dev->dev, NULL);
+	npu = airoha_npu_get(dev->dev);
 	if (IS_ERR(npu)) {
 		request_module("airoha-npu");
-		npu = airoha_npu_get(dev->dev, NULL);
+		npu = airoha_npu_get(dev->dev);
 	}
 
 	if (IS_ERR(npu)) {
